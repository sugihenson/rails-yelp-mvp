<div class="m-4">
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: 1..5, prompt: 'Select a rating' %>
  <%= f.submit %>
<% end %>
</div>